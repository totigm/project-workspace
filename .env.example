# Host port for the Postgres container (docker-compose) and the URL below.
# Override locally if 5432 is already taken on your machine.
POSTGRES_PORT=5432
DATABASE_URL="postgresql://workspace:workspace@localhost:5432/workspace?schema=public"
